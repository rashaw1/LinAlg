// Tester program for Vector class

#include <iostream>
#include "vector.hpp"
#include "matrix.hpp"
#include "error.hpp"
#include <cmath>

;
  for (int i = 0; i < 3; i++){
    std::cout << "d( " << i << " ) = " << dVec[i] << "\n";
  }
  return 0;
}
